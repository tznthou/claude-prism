# OS
.DS_Store
._*
Thumbs.db

# IDE
.vscode/
.idea/
*~
*.swp
*.swo

# Claude Code (local settings, not shared)
.claude/

# Environment (may contain API keys)
.env
.env.*
!.env.example
!.env.*.example

# Logs (local only, not shipped)
*.log

# Internal analysis (not shipped)
internal/

# autoresearch-loop artifacts (local only, never shipped)
.autoresearch-*.md
backup/
iterations/
final-comparison.md

# Security audit reports (local only)
security-reports/

# Node.js (npx distribution wrapper only, no actual deps)
node_modules/
.claude/RESUME.md
